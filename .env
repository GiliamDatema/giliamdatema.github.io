# These env variables are always loaded, both for development and production.
# Mode-specific env will take higher priority.
# Only variables prefixed with VITE_ are exposed to the client source code via import.meta.env.

VITE_APP_TITLE = "Giliam's Recipes"
VITE_APP_VERSION = $npm_package_version